import Immutable from 'immutable';
const initialState = {
};


export default function permissionManage (state=initialState, action) {
    switch(action.type) {
        default:
            return state;
    }
}